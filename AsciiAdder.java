package sprint;

public class AsciiAdder {

    public char addAscii(char input, int intValue) {
        char result = (char) (input + intValue); // result outputs inputed char moved by the int value.
        return result;
    }
}
